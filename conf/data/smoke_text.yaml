task_name: classification
input_kind: text
num_classes: 2
max_length: 64
train_max_length: null
eval_max_length: null
lm_stride: null
train_lm_stride: null
eval_lm_stride: null
train_fraction: 1.0
eval_fraction: 1.0
max_train_examples: null
max_eval_examples: null
split_seed: 1111
label_field: label
loader:
  batch_size: 4
  eval_batch_size: 4
  num_workers: 0
splits:
  train: train
  validation: validation
  test: test
source:
  path: csv
  data_files:
    train: ${paths.data_root}/smoke_text/train.tsv
    validation: ${paths.data_root}/smoke_text/val.tsv
    test: ${paths.data_root}/smoke_text/test.tsv
  sep: "`t"
text:
  primary_field: text
  secondary_field: null
image:
  field: image
  channels: 1
tokenizer:
  type: wordpunct
  name_or_path: null
  min_frequency: 1
  lowercase: true
  add_bos: false
  add_eos: true
