task_name: classification
input_kind: text
num_classes: 2
max_length: 4096
train_max_length: null
eval_max_length: null
lm_stride: null
train_lm_stride: null
eval_lm_stride: null
train_fraction: 1.0
eval_fraction: 1.0
max_train_examples: null
max_eval_examples: null
split_seed: 42
label_field: label
loader:
  batch_size: 8
  eval_batch_size: 8
  num_workers: 4
splits:
  train: train
  validation: test
  test: test
source:
  path: imdb
  cache_dir: ${paths.data_root}/imdb/hf_cache
  local_data_dir: ${paths.data_root}/imdb
text:
  primary_field: text
  secondary_field: null
image:
  field: image
  channels: 1
tokenizer:
  type: char
  name_or_path: null
  min_frequency: 15
  lowercase: false
  add_bos: false
  add_eos: true
