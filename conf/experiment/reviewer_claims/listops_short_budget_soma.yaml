# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: soma
  - _self_

run:
  name: reviewer_short_budget_listops_soma

trainer:
  max_epochs: 5
  limit_train_batches: 0.25
