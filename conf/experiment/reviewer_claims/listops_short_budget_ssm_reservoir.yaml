# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: ssm_reservoir
  - _self_

run:
  name: reviewer_short_budget_listops_reservoir

trainer:
  max_epochs: 5
  limit_train_batches: 0.25
