# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: ssm_reservoir
  - _self_

run:
  name: reviewer_length_generalization_listops_reservoir

data:
  max_length: 1024
  eval_max_length: 2048
