# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: ssm_causal
  - _self_

run:
  name: reviewer_short_budget_listops_causal

trainer:
  max_epochs: 5
  limit_train_batches: 0.25
