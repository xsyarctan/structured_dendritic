# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: ssm_causal
  - _self_

run:
  name: reviewer_low_data_listops_causal

data:
  train_fraction: 0.1
