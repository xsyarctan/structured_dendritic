# @package _global_

defaults:
  - override /task: classification
  - override /data: listops
  - override /model: soma
  - _self_

run:
  name: reviewer_low_data_listops_soma

data:
  train_fraction: 0.1
