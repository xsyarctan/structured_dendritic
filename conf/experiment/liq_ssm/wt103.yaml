# @package _global_

defaults:
  - override /task: language_modeling
  - override /data: wt103
  - override /model: ssm_reservoir
  - _self_

run:
  name: liq_ssm_wt103
optimizer:
  lr: 5.0e-4
trainer:
  max_epochs: 300
model:
  d_model: 512
  n_layers: 12
scheduler:
  warmup_steps: 2000

